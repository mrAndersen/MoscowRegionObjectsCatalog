{% extends '::admin_base.html.twig' %}

{% block body -%}
    <h1>SaleType creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('sale_type') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
