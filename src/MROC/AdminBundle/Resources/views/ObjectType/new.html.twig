{% extends '::admin_base.html.twig' %}

{% block body -%}
    <h1>Создание типа объекта</h1>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_row(form.name) }}
    {{ form_widget(form.save) }}

    {{ form_widget(form._token) }}
{% endblock %}
